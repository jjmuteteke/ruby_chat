<!-- partial to show conversations -->
<div style="height: 400px; overflow-y: auto;">
      <% if @co.any? and @gco.any? %>
          <ul class="media-list">
            <% @co.each do |conversation| %>
                 <%= sync partial: 'user_listconvo', resource: conversation %>

            <% end %>
            <% @gco.each do |conversation| %>
                <%= sync partial: 'user_listgconvo', resource: conversation %>

            <% end %>
          </ul>
          
      <% elsif @gco.any? %>
          <ul class="media-list">
            <% @gco.each do |conversation| %>
                <%= sync partial: 'user_listgconvo', resource: conversation %>

            <% end %>
          </ul>
          
       <% elsif @co.any?  %>
          <ul class="media-list">
            <% @co.each do |conversation| %>
                <%= sync partial: 'user_listconvo', resource: conversation %>

            <% end %>
          </ul>
      
      <% else %>
          <p>You have no conversations. Click send message with any user to create one.</p>
      <% end %>
    </div>
