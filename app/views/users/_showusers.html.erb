  <!-- Default panel contents -->
        <div class="panel-heading">Registered Users</div>

        <!-- Table -->
        <table class="table">
          <thead>
          <tr>
           
            <th>Name</th>
            <th></th>
          </tr>
          </thead>
          <tbody>
          <% @users.each do |user| %>
              <%= sync partial: 'user_listuser', resource: user %>
          <% end %>

          </tbody>
        </table>