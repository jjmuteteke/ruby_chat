  <!-- Default panel contents -->
        <div class="panel-heading">Registered Users</div>

        <!-- Table -->
        <table class="table">
          <thead>
          <tr>
            <th>#</th>
            <th>Name</th>
            <th></th>
          </tr>
          </thead>
          <tbody>
          <% @users.each do |user,index| %>
 <%= sync partial: 'user_listusers', resource: {:user =>user,:index =>index} %>
 
 <% end %>

          </tbody>
        </table>