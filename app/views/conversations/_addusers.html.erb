<!-- this is code to bring down the add users for group convo -->
<!-- change this to a form so that we can use the tokeninput -->

<div class="custom-search-input">
                <div class="input-group">
                    <input type="text" class="form-control input-sm isearchbartype" id="searchbar-typehead" placeholder="add users" autofocus data-cid ="<%=@conversation.id %>" />
                    <div class="results" id="search-results"></div>
                    <span class="input-group-btn">
                        <button class="btn btn-info btn-lg adduser" type="button" data-rip = "<%=@conversation.recipient_id %>" data-cid ="<%=@conversation.id %>" data-sid= <%=current_user.id%> >
                            <i class="fa fa-search-plus"></i> 
                            
                        </button>
                    </span>
                </div>
            </div>
           
<!-- it needs to be something like this-->
